# Backend

This folder contains the backend server, routes, controllers, models, middleware, and configuration for the project.

## Structure

- `controllers/` - request handlers
- `models/` - database models
- `routes/` - API routes
- `middleware/` - custom middleware
- `config/` - configuration files
- `server.js` - backend entry point
